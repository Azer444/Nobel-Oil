@using Manage.Areas.Admin.Models.PageManagement.ExecutiveManagement
@using Manage.Tools.FileHandler.Abstraction
@using Core.Constants.File

@model ExecutiveManagementIndexViewModel
@inject IFileService _fileService

@{
    ViewData["Title"] = "Executive Management";
}

<div class="row">
    <div class="col-sm-12">
        <div class="page-title-box">
            <h4 class="page-title">Executive Management</h4>
        </div>
    </div>
</div>

@if (TempData["message"] != null)
{
    <div class="alert alert-@TempData["message_type"]">
        @TempData["message"]
    </div>
}

<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <div class="table-responsive">
                    <h3>Component</h3>
                    @if (Model.ExecutiveManagementComponent == null)
                    {
                        <a asp-action="definecomponent" asp-controller="executivemanagement" asp-area="admin" asp type="button" class="btn btn-gradient-primary waves-effect waves-light float-right mb-3">
                            Define component
                        </a>
                    }
                    else
                    {
                        <table id="users-table" class="table table-striped mb-0 ">
                            <thead>
                                <tr>
                                    <th>
                                        <span class="mr-1" style="pointer-events: none;">Photo</span>
                                    </th>
                                    <th>
                                        <span class="mr-1" style="pointer-events: none;">@Html.DisplayNameFor(x => x.ExecutiveManagementComponent.Title_AZ)</span>
                                    </th>
                                    <th>
                                        <span class="mr-1" style="pointer-events: none;">@Html.DisplayNameFor(x => x.ExecutiveManagementComponent.Title_RU)</span>
                                    </th>
                                    <th>
                                        <span class="mr-1" style="pointer-events: none;">@Html.DisplayNameFor(x => x.ExecutiveManagementComponent.Title_EN)</span>
                                    </th>
                                    <th>
                                        <span class="mr-1" style="pointer-events: none;">@Html.DisplayNameFor(x => x.ExecutiveManagementComponent.Title_TR)</span>
                                    </th>
                                    <th></th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr>
                                    <td>
                                        @{
                                            var photoPath = _fileService.GetFileUrl(Model.ExecutiveManagementComponent.PhotoName, FilePath.ExecutiveManagement);
                                        }
                                        <img style="width:200px; object-fit:cover;" src="@photoPath" />
                                    </td>
                                    <td>@Model.ExecutiveManagementComponent.Title_AZ</td>
                                    <td>@Model.ExecutiveManagementComponent.Title_RU</td>
                                    <td>@Model.ExecutiveManagementComponent.Title_EN</td>
                                    <td>@Model.ExecutiveManagementComponent.Title_TR</td>
                                    <td>
                                        <a asp-controller="executivemanagement" asp-action="editcomponent" class="mr-2"><i class="fas fa-edit text-info font-18"></i></a>
                                        <a asp-controller="executivemanagement" asp-action="detailscomponent" class="mr-2"><i class="fas fa-external-link-square-alt text-info font-18"></i></a>
                                    </td>
                                </tr>
                            </tbody>
                        </table>
                    }

                </div><!--end /tableresponsive-->
            </div><!--end card-body-->
        </div><!--end card-->
    </div> <!-- end col -->
</div>


@section Scripts{

}