@using Manage.Areas.Admin.Models.PageManagement.Environment
@using Manage.Tools.FileHandler.Abstraction
@using Core.Constants.File

@model EnvironmentIndexViewModel
@inject IFileService _fileService

@{
    ViewData["Title"] = "Environment";
}

<div class="row">
    <div class="col-sm-12">
        <div class="page-title-box">
            <h4 class="page-title">Environment</h4>
        </div>
    </div>
</div>

@if (TempData["message"] != null)
{
    <div class="alert alert-@TempData["message_type"]">
        @TempData["message"]
    </div>
}

<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <div class="table-responsive">
                    <h3>Component</h3>
                    @if (Model.EnvironmentComponent == null)
                    {
                        <a asp-action="definecomponent" asp-controller="environment" asp-area="admin" type="button" class="btn btn-gradient-primary waves-effect waves-light float-right mb-3">
                            Define component
                        </a>
                    }
                    else
                    {
                        <table id="ourproject-table" class="table table-striped mb-0 ">
                            <thead>
                                <tr>
                                    <th>
                                        <span class="mr-1" style="pointer-events: none;">Photo</span>
                                    </th>
                                    <th>
                                        <span class="mr-1" style="pointer-events: none;">@Html.DisplayNameFor(x => x.EnvironmentComponent.Title_AZ)</span>
                                    </th>
                                    <th>
                                        <span class="mr-1" style="pointer-events: none;">@Html.DisplayNameFor(x => x.EnvironmentComponent.Title_RU)</span>
                                    </th>
                                    <th>
                                        <span class="mr-1" style="pointer-events: none;">@Html.DisplayNameFor(x => x.EnvironmentComponent.Title_EN)</span>
                                    </th>
                                    <th>
                                        <span class="mr-1" style="pointer-events: none;">@Html.DisplayNameFor(x => x.EnvironmentComponent.Title_TR)</span>
                                    </th>
                                    <th></th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr>
                                    <td>
                                        @{
                                            var photoPath = _fileService.GetFileUrl(Model.EnvironmentComponent.PhotoName, FilePath.Environment);
                                        }
                                        <img src="@photoPath" style="width:200px;object-fit:cover;" />
                                    </td>
                                    <td>@Model.EnvironmentComponent.Title_AZ</td>
                                    <td>@Model.EnvironmentComponent.Title_RU</td>
                                    <td>@Model.EnvironmentComponent.Title_EN</td>
                                    <td>@Model.EnvironmentComponent.Title_TR</td>
                                    <td>
                                        <a asp-controller="environment" asp-action="editcomponent" class="mr-2"><i class="fas fa-edit text-info font-18"></i></a>
                                        <a asp-controller="environment" asp-action="detailscomponent" class="mr-2"><i class="fas fa-external-link-square-alt text-info font-18"></i></a>
                                    </td>
                                </tr>
                            </tbody>
                        </table>
                    }

                </div><!--end /tableresponsive-->
            </div><!--end card-body-->
        </div><!--end card-->
        <div class="card">
            <div class="card-body">
                <div class="table-responsive">
                    <h3>Sub Component</h3>
                    <a asp-action="addsubcomponent" asp-controller="environment" asp-area="admin" asp type="button" class="btn btn-gradient-primary waves-effect waves-light float-right mb-3">
                        Add sub-component
                    </a>
                    <table id="ourproject-table" class="table table-striped mb-0 ">
                        <thead>
                            <tr>
                                <th>
                                    <span class="mr-1" style="pointer-events: none;">Photo_Environment</span>
                                </th>
                                <th>
                                    <span class="mr-1" style="pointer-events: none;">@Html.DisplayNameFor(x => x.EnvironmentComponent.Title_AZ)</span>
                                </th>
                                <th>
                                    <span class="mr-1" style="pointer-events: none;">@Html.DisplayNameFor(x => x.EnvironmentComponent.Title_RU)</span>
                                </th>
                                <th>
                                    <span class="mr-1" style="pointer-events: none;">@Html.DisplayNameFor(x => x.EnvironmentComponent.Title_EN)</span>
                                </th>
                                <th>
                                    <span class="mr-1" style="pointer-events: none;">@Html.DisplayNameFor(x => x.EnvironmentComponent.Title_TR)</span>
                                </th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var environmentSubComponent in Model.EnvironmentSubComponents)
                            {
                                <tr>
                                    <td>
                                        @{
                                            var photoPath_ = _fileService.GetFileUrl(environmentSubComponent.PhotoName_Environment, FilePath.Environment);
                                        }
                                        <img src="@photoPath_" style="width:200px;object-fit:cover;" />
                                    </td>
                                    <td>@environmentSubComponent.Title_AZ</td>
                                    <td>@environmentSubComponent.Title_RU</td>
                                    <td>@environmentSubComponent.Title_EN</td>
                                    <td>@environmentSubComponent.Title_TR</td>
                                    <td>
                                        <a asp-controller="environment" asp-action="editsubcomponent" asp-route-id="@environmentSubComponent.Id" class="mr-2"><i class="fas fa-edit text-info font-18"></i></a>
                                        <a asp-controller="environment" asp-action="detailssubcomponent" asp-route-id="@environmentSubComponent.Id" class="mr-2"><i class="fas fa-external-link-square-alt text-info font-18"></i></a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div><!--end /tableresponsive-->

            </div><!--end card-body-->
        </div><!--end card-->

    </div> <!-- end col -->
</div>


@section Scripts{

}